# Ignore only the project's virtual environment directory and dotenv files

# Virtual environment (project-created)
env/
/env/

# dotenv environment files
.env
.env.*

# Nothing else is ignored so all other files will be tracked by Git